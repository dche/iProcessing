
Processing Touch
----------------

What Processing Touch is NOT

It is not a processignjs like porting of Processing. Processing Touch is not
able to execute Processing's java style code. You must write your sketch in
Objective-C.

Specification mismatch
----------------------

1. When using the QUARTZ2D mode, the |arc| method will draw a line
   frome the center of the circle to the end point of the arc.
1. PFont type is not used. Use UIFont instead. Hence the loadFont method is
   not supported.
1. Text mode SHAPE is not supported.
1. You can't use tint(255, alpha) to make a image transparency.
1. Following methods are not supported:
    background(gray)
    background(PImage)
    fill(gray)
    stroke(gray)
    tint(gray)
    background(hex, alpha)
    fill(hex, alpha)
    stroke(hex, alpha)
    tint(hex, alpha)



